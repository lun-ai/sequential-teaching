name: psychopy
channels:
- conda-forge
dependencies:
- python=3.6
- psychopy
- pip
- pip:
# need to manually install psychotoolbox to prevent compatiability issues
#  - psychtoolbox==3.0.16
  - pygame
  - pyo
  - pyparallel; platform_system != "Windows"
  - SoundFile; platform_system == "Windows"
  - websocket_client